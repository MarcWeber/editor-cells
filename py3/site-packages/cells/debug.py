#!/usr/bin/env python
import sys

debug = False
def debug_str(s):
    # also print to vim's :messages trace
    f = open('/tmp/zzz', 'a')
    f.write("debugs %s\n"% s)
    f.close()
    if debug:
        sys.stderr.write("%s" % s)

