#!/usr/bin/env python
import sys
import os

debug = False
def debug_str(s):
    # also print to vim's :messages trace
    f = open('/tmp/editor-cells-log-py-%s' % os.environ["USER"], 'a')
    f.write("debugs %s\n"% s)
    f.close()
    if debug:
        sys.stderr.write("%s" % s)

